# TextFormatter
A quick script for turning mangled data where columns got squished into rows into a well formatted CSV.

Say the number of cols, specify the path to file, then get your result output as a CSV.
